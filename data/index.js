const userData = require('./users');
const donationData = require('./donations');
const addressData = require('./addresses');
// const roleData = require('./roles');

module.exports = {
  users: userData,
  donations: donationData,
  addresses: addressData,
  // roles: roleData
};
